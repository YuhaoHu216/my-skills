# ============================================================
# Caddy 网关配置：域名 → 反代到前端 Nginx 容器（TLS 自动签发）
# 替换占位符：{{DOMAIN}} {{PROJECT}}
# 每行一个站点；放到项目 caddy/Caddyfile 或共享 caddy 项目
# ============================================================
{{DOMAIN}} {
	reverse_proxy {{PROJECT}}-web:5173
}
