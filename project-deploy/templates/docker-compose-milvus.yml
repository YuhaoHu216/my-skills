# ============================================================
# Milvus 向量库集群：etcd + minio + milvus + attu（管理台）
# 需要向量检索时才部署。内存占用大（milvus 建议 2G+），小内存服务器慎用
# 示例值仅为示例，实际以用户确认的参数为准
# ============================================================
services:
  etcd:
    image: quay.io/coreos/etcd:v3.5.18
    container_name: etcd-milvus
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - etcd_data:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - shared-infra

  minio:
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    container_name: minio-milvus
    environment:
      MINIO_ROOT_USER: minioadmin        # 按需改
      MINIO_ROOT_PASSWORD: minioadmin    # 按需改
    volumes:
      - minio_data:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - shared-infra

  milvus:
    image: milvusdb/milvus:v2.5.10
    container_name: milvus
    command: milvus run standalone
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
      MINIO_ACCESS_KEY_ID: minioadmin
      MINIO_SECRET_ACCESS_KEY: minioadmin
    volumes:
      - milvus_data:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    networks:
      - shared-infra
    deploy:
      resources:
        limits:
          memory: 2G
    depends_on:
      etcd:
        condition: service_healthy
      minio:
        condition: service_healthy

  attu:
    image: zilliz/attu:v2.5
    container_name: attu
    ports:
      - "8000:3000"
    environment:
      MILVUS_URL: milvus:19530
    depends_on:
      - milvus
    networks:
      - shared-infra

networks:
  shared-infra:
    name: shared-infra
    external: true

volumes:
  etcd_data:
  minio_data:
  milvus_data:
